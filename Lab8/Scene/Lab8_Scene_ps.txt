#version 300 es

#ifdef GL_ES
precision highp float;
#endif // GL_ES

in vec2 vTexcoord;

uniform sampler2D uTex;

layout (location = 0) out vec4 rtFragColor;

in vec4 vNormal;

void main ()
{
	//rtFragColor = vNormal;//vec4(1.0, 1.0, 1.0, 1.0);
	rtFragColor = texture(uTex, vTexcoord);
	//rtFragColor = vec4(0.5, 0.5, 0.5, 1.0);
}