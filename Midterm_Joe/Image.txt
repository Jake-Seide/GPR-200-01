// mainImage: process the current pixel (exactly one call per pixel)
//    fragColor: output final color for current pixel
//    fragCoord: input location of current pixel in image (in pixels)
void mainImage(out vec4 fragColor, in  vec2 fragCoord)
{
    // setup
    // test UV for input image
    vec2 uv = fragCoord / iChannelResolution[0].xy;
    
    // TESTING
    // set iChannel0 to 'Misc/Buffer A' and fetch sample
    fragColor = texture(iChannel0, uv);
    //fragColor = texture(iChannel1, uv);
    //fragColor = ImageComposite(uv);
}

// Tips for optimazation tommorrow
// Look through the small parts of our math
// Such as with pow and abs (ask "Do we really need that?") or is there a simple answer right around the corner
// Even if its close such as with the pow change on Buffer B